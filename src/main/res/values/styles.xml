<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--全局按钮样式-->
    <style name="Button">
        <item name="android:layout_height">50dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
    </style>

    <style name="Button.Menu">
        <item name="android:background">@color/Transparent</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <style name="Button.Menu.WithICO">
        <item name="android:drawableRight">@drawable/ic_right</item>
        <item name="android:drawableEnd">@drawable/ic_right</item>
    </style>

    <style name="Button.Standard">
        <item name="android:background">@drawable/button_shaps</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/White</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="Button.Normal">
        <item name="android:textSize">18sp</item>
        <item name="android:background">@drawable/button_shaps_normal</item>
        <item name="android:textColor">@color/Black</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

</resources>
